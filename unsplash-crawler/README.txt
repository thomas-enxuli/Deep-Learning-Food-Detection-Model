Python script for crawling unsplash.com, extracting collection and photo metadata, and for downloading the actual images.

Note: all the crawled data goes into the `photos` directory, which is gitignored. The contents of that directory should be manually synced with s3://unsplash-photos/

